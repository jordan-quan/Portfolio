import React from 'react'

import * as styles from './styles'

const HomeButton = () => (
  <styles.BackWrapper>
    <styles.Back to={`/`} style={{ float: 'left' }}>
      jq
    </styles.Back>
  </styles.BackWrapper>
)

export default HomeButton
